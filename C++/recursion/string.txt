#include<iostream>
using namespace std;

string reverse(string st)
{
    
    if(st.length()==0)
    {
        return r;
    
    }
    return r;
    string r = r+st[st.length-1];
}
int main()
{
    reverse("ssigj");
}